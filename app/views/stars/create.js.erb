var star = $('#star');
star.attr('href', '<%= escape_javascript(star_path(@star)) %>')
	.data('method', 'delete')
    .removeClass('btn-primary')
    .addClass('btn-danger')
    .text('Remove from favorites');