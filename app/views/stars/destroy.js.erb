var star = $('#star');
star.attr('href', '<%= escape_javascript(stars_path(@restaurant)) %>')
	.data('method', 'post')
    .removeClass('btn-danger')
    .addClass('btn-success')
    .text('Add to favorites');